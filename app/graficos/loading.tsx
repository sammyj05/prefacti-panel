import { CargandoPantalla } from "@/components/ui/Cargando";

/** Las barras por promoción, con el conmutador de métrica arriba. */
export default function Cargando() {
  return <CargandoPantalla acciones={1} forma="bloque" filas={8} />;
}
