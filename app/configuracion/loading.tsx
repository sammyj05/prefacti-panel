import { CargandoPantalla } from "@/components/ui/Cargando";

/** Los umbrales, la apariencia y el equipo. */
export default function Cargando() {
  return <CargandoPantalla acciones={0} forma="bloque" filas={6} />;
}
