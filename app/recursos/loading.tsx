import { CargandoPantalla } from "@/components/ui/Cargando";

/** El archivo: una carpeta plegada por promoción. */
export default function Cargando() {
  return <CargandoPantalla acciones={1} forma="bloque" filas={6} />;
}
